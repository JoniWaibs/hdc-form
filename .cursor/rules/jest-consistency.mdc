---
description: 
globs: 
alwaysApply: true
---
---
description: Consistency rules for Jest testing
globs: "**/*.{test,spec}.{ts,tsx,js,jsx}"
alwaysApply: true
---

# Jest Testing Standards

- Replace all instances of `it()` with `test()`.
- Identify repetitive test patterns and refactor them using `test.each` with a test cases array.
- Ensure test names are descriptive and clear.
- Use `describe()` to group related tests when appropriate.